---
phase: 10-research-distribution-and-cost-calibration
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - bin/README.md
  - config.toml
autonomous: false
requirements:
  - DOCS-05
  - FIX-07

must_haves:
  truths:
    - "bin/README.md lists all 6 v2.0 programs with purpose, cron schedule, inputs, and output contract"
    - "All 4 new cron YAMLs are registered in the cron database after cron-sync"
    - "NixOS rebuild completes successfully with all new crons active"
    - "max_cost_per_day_cents in config.toml is set to a calibrated value derived from measured spend (not default 500)"
  artifacts:
    - path: "bin/README.md"
      provides: "Complete program index for all v2.0 programs"
      contains: "init-state-db.ts"
    - path: "config.toml"
      provides: "Calibrated cost cap"
      contains: "max_cost_per_day_cents"
  key_links:
    - from: "cron/jobs/*.yaml"
      to: "cron database"
      via: "cron-sync"
      pattern: "cron-sync"
    - from: "config.toml"
      to: "zeroclaw runtime"
      via: "NixOS rebuild activation"
      pattern: "max_cost_per_day_cents"
---

<objective>
Update bin/README.md with all v2.0 programs, run cron-sync to register new YAMLs, perform NixOS rebuild, and calibrate cost cap after measurement.

Purpose: Complete documentation, activate all Phase 10 crons, and set a data-driven cost cap.
Output: Updated README, active crons, calibrated config.toml.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-research-distribution-and-cost-calibration/10-CONTEXT.md
@.planning/phases/10-research-distribution-and-cost-calibration/10-RESEARCH.md
@.planning/phases/10-research-distribution-and-cost-calibration/10-01-SUMMARY.md

@bin/README.md
@config.toml

<interfaces>
<!-- bin/README.md current state: only lists sentinel-scan.ts and repair-loop.sh -->
<!-- Programs to document: init-state-db.ts, notify.ts, orchestrate.ts, self-audit.ts, sentinel-scan.ts, zai-proxy.ts -->
<!-- repair-loop.sh was removed in Phase 6 — remove from README -->
<!-- config.toml max_cost_per_day_cents currently 500 (line 48) -->
<!-- config.toml requires NixOS rebuild to take effect -->
<!-- cron-sync registers YAML files into cron database -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite bin/README.md and activate crons via cron-sync + rebuild</name>
  <files>bin/README.md</files>
  <action>
Rewrite bin/README.md to be a complete program index for all v2.0 programs. Keep the existing header structure (When to Use, Standards sections) but replace the Programs table.

**Remove:** repair-loop.sh entry (deleted in Phase 6).

**Add all 6 v2.0 programs with these fields per entry:**
- Program filename
- Purpose (1-line)
- Cron schedule (if any, or "called by other programs" / "manual")
- Inputs (what it reads — CLI args, env vars, files)
- Output contract (what it produces — stdout format, side effects)

Programs to document:
1. `init-state-db.ts` — Initialize SQLite state.db with WAL mode and v3 schema. No cron (called by other programs at startup). Input: optional db-path arg. Output: JSON `{"schema_version": 3}` to stdout.
2. `notify.ts` — Send WhatsApp message with retry and rate limiting. No cron (called by other programs). Input: --to phone --text message (CLI flags). Output: JSON `{"sent": true}` to stdout, logs to notify_log table.
3. `orchestrate.ts` — Execute multi-step agent tasks from YAML via claude -p. Cron: auto-generated for type: agent YAMLs. Input: YAML file path + optional --db-path. Output: JSON task result, checkpoints to orchestration_tasks table.
4. `self-audit.ts` — Detect config/doc drift between git source and deployed state. Cron: weekly (self-audit.yaml). Input: none. Output: JSON drift report to stdout, WhatsApp alert if drift detected.
5. `sentinel-scan.ts` — Scan memory for unresolved issues, alert via WhatsApp. Cron: every 2 hours (sentinel.yaml). Input: --notify phone flag. Output: JSON scan results, WhatsApp alert if issues found.
6. `zai-proxy.ts` — ZAI API proxy server. No cron (runs as service). Input: config.toml settings. Output: HTTP proxy on port 5100.

After writing README, run cron-sync to register the 4 new YAML files and perform NixOS rebuild:
```bash
cron-sync
sudo nixos-rebuild switch --impure --option eval-cache false --flake /etc/nixos#nixos
```
  </action>
  <verify>
    <automated>cd /etc/nixos/zeroclaw && grep -c "init-state-db.ts\|notify.ts\|orchestrate.ts\|self-audit.ts\|sentinel-scan.ts\|zai-proxy.ts" bin/README.md</automated>
  </verify>
  <done>bin/README.md lists all 6 v2.0 programs with purpose, cron schedule, inputs, and output contract. repair-loop.sh removed. cron-sync registered new YAMLs. NixOS rebuild completed successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify crons active and measure cost for calibration</name>
  <files>config.toml</files>
  <action>
Human checkpoint -- verify all 4 new crons are registered, let them run for 3+ days, measure actual daily spend, and report the calibrated cost cap value.

What was built -- All Phase 10 cron jobs registered and NixOS rebuild applied. Four new agent crons are scheduled: build-in-public drafter (Mon/Wed/Fri 09:00), engagement scout (daily 10:00), paper scout (Mon 09:00), company refresh (Wed 10:00).

How to verify:
1. Run `zeroclaw cron list` to verify all 4 new crons appear
2. Let all crons run for 3+ consecutive days
3. Check ZAI dashboard/billing for daily token spend in cents
4. Calculate average daily spend x 2 = new max_cost_per_day_cents value
5. Report the measured value so Claude can update config.toml

Resume signal -- Provide the calibrated max_cost_per_day_cents value (e.g., "set cost cap to 1200")
  </action>
  <verify>
    <automated>zeroclaw cron list | grep -c "Build-in-Public\|Engagement\|Paper\|Company"</automated>
  </verify>
  <done>All 4 Phase 10 crons confirmed active in cron list, user has measured daily spend and provided calibrated cost cap value</done>
</task>

<task type="auto">
  <name>Task 3: Set calibrated cost cap in config.toml</name>
  <files>config.toml</files>
  <action>
Update `max_cost_per_day_cents` in `/etc/nixos/zeroclaw/config.toml` from 500 to the user-provided calibrated value (2x measured average daily spend).

The value is a plain integer on line 48 (no @PLACEHOLDER@ token). Use sed or direct edit to replace the number.

After editing, run NixOS rebuild to activate:
```bash
sudo nixos-rebuild switch --impure --option eval-cache false --flake /etc/nixos#nixos
```

Verify the change took effect:
```bash
grep max_cost_per_day_cents /etc/nixos/zeroclaw/config.toml
```
  </action>
  <verify>
    <automated>grep "max_cost_per_day_cents" /etc/nixos/zeroclaw/config.toml | grep -v "500"</automated>
  </verify>
  <done>max_cost_per_day_cents is set to a calibrated value derived from 3+ days of measured spend, not the default 500. NixOS rebuild applied.</done>
</task>

</tasks>

<verification>
- `cat bin/README.md` shows all 6 programs documented
- `zeroclaw cron list` shows all 4 new Phase 10 crons
- `grep max_cost_per_day_cents config.toml` shows calibrated value (not 500)
- `cd /etc/nixos/zeroclaw && bun test bin/` — full test suite passes
</verification>

<success_criteria>
- bin/README.md is a complete index of all v2.0 programs with purpose, schedule, inputs, output contract
- repair-loop.sh is no longer listed
- All 4 Phase 10 crons are registered and active
- max_cost_per_day_cents reflects measured empirical data (2x average daily spend)
- NixOS rebuild completed successfully
</success_criteria>

<output>
After completion, create `.planning/phases/10-research-distribution-and-cost-calibration/10-02-SUMMARY.md`
</output>
