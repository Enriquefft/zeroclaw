---
phase: 10-research-distribution-and-cost-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cron/jobs/build-in-public-drafter.yaml
  - cron/jobs/engagement-scout.yaml
  - cron/jobs/paper-scout.yaml
  - cron/jobs/company-refresh.yaml
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-04

must_haves:
  truths:
    - "Four new agent cron YAML files exist and parse correctly via orchestrate.ts parseYaml"
    - "Build-in-public drafter runs Mon/Wed/Fri at 09:00 Lima, sources git log + state.db, drafts X and LinkedIn versions, delivers via WhatsApp"
    - "Engagement scout runs daily at 10:00 Lima, searches X/Reddit/HN, drafts replies in Enrique's voice, silent on zero results"
    - "Paper scout runs weekly Monday 09:00 Lima, fetches 5 arXiv categories, filters by research interests, delivers top-5 digest"
    - "Company refresh runs weekly Wednesday 10:00 Lima, mines state.db + web for companies, updates LORE.md Target Companies section"
  artifacts:
    - path: "cron/jobs/build-in-public-drafter.yaml"
      provides: "Build-in-public draft generation cron"
      contains: "type: agent"
    - path: "cron/jobs/engagement-scout.yaml"
      provides: "Engagement thread discovery cron"
      contains: "type: agent"
    - path: "cron/jobs/paper-scout.yaml"
      provides: "Weekly arXiv digest cron"
      contains: "type: agent"
    - path: "cron/jobs/company-refresh.yaml"
      provides: "Weekly company watchlist refresh cron"
      contains: "type: agent"
  key_links:
    - from: "cron/jobs/build-in-public-drafter.yaml"
      to: "orchestrate.ts"
      via: "cron-sync auto-generates command"
      pattern: "type: agent"
    - from: "cron/jobs/company-refresh.yaml"
      to: "documents/LORE.md"
      via: "agent step writes Target Companies section"
      pattern: "Target Companies"
---

<objective>
Create four agent cron YAML files for research and distribution automation: build-in-public drafter, engagement scout, paper scout, and weekly company refresh.

Purpose: Wire the final four Heartbeat crons that complete the v2.0 automated assistant capabilities.
Output: 4 YAML files in cron/jobs/ following established type: agent pattern.
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-research-distribution-and-cost-calibration/10-CONTEXT.md
@.planning/phases/10-research-distribution-and-cost-calibration/10-RESEARCH.md

@cron/jobs/content-scout.yaml
@cron/jobs/job-scanner.yaml

<interfaces>
<!-- Existing YAML pattern from content-scout.yaml and job-scanner.yaml -->
<!-- All agent YAMLs follow this structure: name, schedule, tz, type: agent, goal, steps[], notify (optional) -->
<!-- cron-sync auto-generates command field for type: agent jobs -->
<!-- parseYaml regex: no inner double-quotes in steps, no key: value colon-space patterns -->
<!-- Use -- dash separator instead of colon for labels in step text -->
<!-- Steps reference files by absolute path: /etc/nixos/zeroclaw/documents/LORE.md -->
<!-- Silent pattern: "output SILENT and stop" when nothing actionable -->
<!-- WhatsApp send: kapso-whatsapp-cli send --to +51926689401 --text '<message>' -->
<!-- state.db path: ~/.zeroclaw/workspace/state.db -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create build-in-public drafter and engagement scout YAMLs</name>
  <files>cron/jobs/build-in-public-drafter.yaml, cron/jobs/engagement-scout.yaml</files>
  <action>
Create two agent cron YAML files following the established pattern from content-scout.yaml and job-scanner.yaml.

**build-in-public-drafter.yaml (DIST-01):**
- name: "Build-in-Public Drafter"
- schedule: "0 9 * * 1,3,5" (Mon/Wed/Fri 09:00 Lima)
- tz: "America/Lima"
- type: agent
- goal: Summarize what it does (draft X + LinkedIn posts from git activity for human approval)
- 4 steps:
  1. Run git log --since=2.days.ago --oneline in /etc/nixos/zeroclaw for recent commits. Query state.db at ~/.zeroclaw/workspace/state.db for job_applications with status changes in the last 2 days (last_updated >= unixepoch() - 172800). Also query content_log for recent entries to avoid repeating covered commits. Output structured activity summary.
  2. Read /etc/nixos/zeroclaw/documents/LORE.md content pillars and platform strategy. Assess if activity is substantive -- at least 1 meaningful commit or 1 job hunt milestone. If not enough, output SILENT and stop.
  3. Draft X version using LORE.md voice guide -- short punchy opinionated max 280 chars. Draft LinkedIn version -- longer professional framing 400-800 chars. Label each draft clearly with DRAFT FOR X and DRAFT FOR LINKEDIN separators.
  4. Send both drafts to +51926689401 via kapso-whatsapp-cli send --to +51926689401 --text with both drafts and which commits/milestones each covers.
- notify: "+51926689401"

**engagement-scout.yaml (DIST-02):**
- name: "Engagement Scout"
- schedule: "0 10 * * *" (daily 10:00 Lima)
- tz: "America/Lima"
- type: agent
- goal: Summarize what it does (find threads on X/Reddit/HN where Enrique can add value, draft replies)
- 4 steps:
  1. Search the web for recent threads mentioning AI agents, build-in-public developer, LLM engineering, orchestration systems. Focus on X, Reddit (r/MachineLearning, r/LocalLLaMA, r/programming), and HackerNews. Collect top 3-5 threads with title, URL, and brief context.
  2. Read /etc/nixos/zeroclaw/documents/LORE.md platform strategy and voice guide. Filter threads -- keep only those where a technical reply from an AI engineer adds genuine value. Discard promotional threads or those with nothing unique to contribute. If none qualify, output SILENT and stop.
  3. For each qualifying thread, draft a reply in Enrique's voice per LORE.md -- genuine technical depth, not promotional, references concrete experience. Under 280 chars for X, longer for Reddit/HN.
  4. Send qualifying threads and draft replies to +51926689401 via kapso-whatsapp-cli send --to +51926689401 --text with each thread URL on one line followed by the draft reply.
- notify: "+51926689401"

**CRITICAL YAML rules (from Phase 08/09 learnings):**
- No inner double-quotes in step strings (parseYaml truncates at first inner quote)
- No `key: value` colon-space patterns in step text (Go YAML parser rejects them)
- Use `--` dash separator for labels (e.g., "DRAFT FOR X -- content" not "DRAFT FOR X: content")
- Do NOT include a `command:` field on type: agent YAMLs (cron-sync auto-generates it)
  </action>
  <verify>
    <automated>cd /etc/nixos/zeroclaw && bun test bin/orchestrate.test.ts</automated>
  </verify>
  <done>Both YAML files exist, parse correctly via parseYaml (existing tests pass), follow established agent pattern with correct schedules and step structure</done>
</task>

<task type="auto">
  <name>Task 2: Create paper scout and company refresh YAMLs</name>
  <files>cron/jobs/paper-scout.yaml, cron/jobs/company-refresh.yaml</files>
  <action>
Create two weekly agent cron YAML files.

**paper-scout.yaml (DIST-03):**
- name: "Paper Scout"
- schedule: "0 9 * * 1" (Monday 09:00 Lima)
- tz: "America/Lima"
- type: agent
- goal: Summarize what it does (fetch arXiv feeds, filter by research interests, deliver top-5 weekly digest)
- 4 steps:
  1. Fetch arXiv RSS feeds -- cs.AI at https://arxiv.org/rss/cs.AI, cs.LG at https://arxiv.org/rss/cs.LG, cs.CL at https://arxiv.org/rss/cs.CL, cs.MA at https://arxiv.org/rss/cs.MA, cs.SE at https://arxiv.org/rss/cs.SE. Extract title, authors, abstract, and URL for papers from the last 7 days. Output a list of collected papers.
  2. Read /etc/nixos/zeroclaw/documents/LORE.md research interests section. Filter papers against these interests -- multi-agent systems in adversarial environments, algorithmic fairness in public systems, LLM reasoning over structured data, compilers and LLMs, spec-driven development. Rank by relevance and select the top 5. If none qualify, output SILENT and stop.
  3. For each of the top 5 papers, write a 2-3 sentence summary covering main contribution, key result, and relevance to the research interests. Format each as title -- authors -- summary -- URL.
  4. Send the digest to +51926689401 via kapso-whatsapp-cli send --to +51926689401 --text with a WEEKLY PAPER DIGEST header followed by the 5 paper entries.
- notify: "+51926689401"

**company-refresh.yaml (DIST-04):**
- name: "Company Refresh"
- schedule: "0 10 * * 3" (Wednesday 10:00 Lima)
- tz: "America/Lima"
- type: agent
- goal: Summarize what it does (mine state.db + web for target companies, update LORE.md Target Companies section)
- 4 steps:
  1. Query state.db at ~/.zeroclaw/workspace/state.db for company names. Run sqlite3 ~/.zeroclaw/workspace/state.db with SELECT DISTINCT company FROM job_applications WHERE company IS NOT NULL UNION SELECT DISTINCT company FROM freelance_leads WHERE company IS NOT NULL. Output the deduplicated company list.
  2. Read /etc/nixos/zeroclaw/documents/LORE.md Target Companies section and Target Paths section. Identify which categories have fewer than 5 companies -- Remote-Global, US Sponsors (H-1B/O-1), Relocation-Friendly. Search the web for companies in underrepresented categories matching remote-first hiring engineers LATAM-friendly, known H-1B sponsors for software engineers, relocation-friendly companies. Collect name, category, and 1-2 recent hiring signals per company.
  3. Merge state.db companies and web-researched companies into a consolidated list. For each company note its Target Path category and any recent news or hiring signals. Limit to 10-15 companies total across all categories.
  4. Read the complete contents of /etc/nixos/zeroclaw/documents/LORE.md. Identify the section starting with ## Target Companies and ending at the next ## heading. Replace only that section with the updated content from step 3, formatted with three subsections -- Remote-Global, US Sponsors (H-1B / O-1), Relocation-Friendly -- each with bullet entries. Write the complete modified file back to /etc/nixos/zeroclaw/documents/LORE.md. Send a summary of companies added per category to +51926689401 via kapso-whatsapp-cli send --to +51926689401 --text.
- notify: "+51926689401"

**Same YAML rules apply:** No inner double-quotes, no colon-space key-value patterns, use -- dash separators, no command: field.
  </action>
  <verify>
    <automated>cd /etc/nixos/zeroclaw && bun test bin/orchestrate.test.ts</automated>
  </verify>
  <done>Both YAML files exist, parse correctly via parseYaml, paper-scout has 5 arXiv categories and weekly Monday schedule, company-refresh has safe LORE.md section-replace pattern in step 4</done>
</task>

</tasks>

<verification>
All 4 YAML files exist in cron/jobs/:
- `ls cron/jobs/build-in-public-drafter.yaml cron/jobs/engagement-scout.yaml cron/jobs/paper-scout.yaml cron/jobs/company-refresh.yaml`
- `cd /etc/nixos/zeroclaw && bun test bin/orchestrate.test.ts` passes (parseYaml validates structure)
- Each file has: name, schedule, tz: America/Lima, type: agent, goal, steps array, no command field
</verification>

<success_criteria>
- 4 new YAML files created following established agent cron pattern
- All existing tests pass (parseYaml covers structural validation)
- Schedules correct: drafter Mon/Wed/Fri 09:00, engagement daily 10:00, paper scout Mon 09:00, company refresh Wed 10:00
- All steps avoid inner double-quotes and colon-space patterns
- Company refresh step 4 uses section-replace pattern (not full LORE.md overwrite)
</success_criteria>

<output>
After completion, create `.planning/phases/10-research-distribution-and-cost-calibration/10-01-SUMMARY.md`
</output>
