[skill]
name = "email"
description = "Full email control across all of Enrique's accounts"
version = "0.2.0"
author = "kiro"
tags = ["email", "gmail", "spacemail", "communication"]

[[tools]]
name = "email_cli"
description = "Unified email CLI. Subcommands: list, search, get, send, reply, forward, thread, labels, label, delete, archive, trash, mark, attachments. Routes to gog gmail (Gmail) or himalaya (SpaceMail) automatically. All output is JSON. Use --account to target a specific account."
kind = "shell"
command = "bun run /etc/nixos/zeroclaw/skills/email/cli.ts"
