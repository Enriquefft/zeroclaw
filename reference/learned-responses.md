# Learned Responses

Append-only log of answers from form-filling sessions. Each entry captures a Q&A that contained new information not already in the profile files.

Entries are added automatically by `form_filler evolve <slug>` after each application.

**To curate:** review entries below, promote good ones into `reusable-responses.md` (as polished templates) or `full-profile.md` (as facts/experience). Delete entries here once promoted.

---
